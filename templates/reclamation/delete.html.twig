<form method="post" action="{{ path('/admin/reclamations/{id}/delete', {'id': object.id}) }}" onsubmit="return confirm('Are you sure you want to delete this complaint ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ object.id) }}">
    <button class="btn">Delete</button>
</form>